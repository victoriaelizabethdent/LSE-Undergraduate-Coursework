# Q3 Movie (...continuing from Q2, 30 marks)

---

## Instructions

In this question, we will continue to work on the data and variables we have created from Q2, and continue to write code in `src/movie.py`.

Note:

- Please do not reload the data or recreate the variables like `movie_data` - just continue to use them
- You may find the functions written in `util.py` useful

1. (12 marks) Write code in [`src/movie.py`](src/movie.py) to find out, based on the given dataset, if most of the actors have participated in many different movies. We will do so by the following steps:

   - Step 1: Create a variable `actor_to_movie_count` which stores the information about the number of movies each actor has participated in. It is a `dict` with actors as the keys (type: `str`) and the corresponding counts of movies s/he has participated in as the corresponding value
   - Step 2: Create a variable `movie_count_to_num_actors` using `actor_to_movie_count` from Step (1). It stores the information about how many actors have participated in $n$ movies. It is a `dict` with the number of movies as the keys (type: `int`), and the value is the number of actors has participated in the corresponding nuber of movies (type: `int`).

     For example, suppose we have a very small data set, with only 1 actor participating in 20 movies, 3 actors participated in 18 movies, then `movie_count_to_num_actors` will have the value `{20: 1, 18: 3}`

   - Step 3: Print out the key-value pairs of `movie_count_to_num_actors` ordered by the keys in ascending order

     For example, suppose we have `movie_count_to_num_actors` as `{20: 1, 18: 3}`, then the expected printout should be something like:

     ```
     18: 3
     20: 1
     ```

     Hint: you may find the built-in function `sorted()` useful

   - Step 4: What do you observe about the number of movies that actors have participated in from the printout of Step (3)? Please write your textual answers below:

     - Observations: **We observed from this output that the highest number of actors (42290) have appeared in only 1 movie, while the lowest number of actors (1) have starred in 21 and 23 movies each, marking quite stark extremes. There is also a notable inverse relationship between the number of actors and the number of movies they have starred in, with fewer actors starring in a higher number of movies. The one anomaly in this otherwise consistent pattern is the number of actors starring in 25 movies, where the count of actors (2) was marginally higher than the 2 preceding counts (1). We have also acknowledged that the dataset lacks information on actors who have participated in 22 and 24 films, posing potential gaps in our understanding of the spread of the output.**

2. (18 marks) Write code in [`src/movie.py`](src/movie.py) to find out the pairs of actors that have worked together in more than 10 movies. Please print out the results. What do you observe? Please write your textual answers below:

- Observations: **From this information, we inferred certain groups of actor pairings and the types of movies they have acted in together. Graham Chapman, Terry Jones, Michael Palin, John Cleese, Terry Gilliam, and Eric Idle have all worked together in various different pair permutations in a range of 11-14 movies together, and upon further inspection we found out all these movies are of the Monty Python movie series. We found similar grouping with John Paul Tremblay, Robb Wells, and Mike Smith who have worked together in various pair permutations in 13/14 movies, a majority of them in the Trailer Park Boys movie series. The pair which has worked in the most number of movies together is Jan Suter and Raul Campos, with 19 movies in common. The pair has no common movies with any other actor in the given dataset, leading us to believe the pair as a regular duo act, also considering the fact that the movies they have starred in together have not all been a part of a certain movie series.**

---

## Note

- For part (1), you can use the built-in function `sorted()`. Feel free to check the official document on how to use it
- You can write some helper functions in [`src/util.py`](src/util.py), or other `.py` files as modules if you want to
  - However, please make sure the required variables like `actor_to_movie_count` are defined in `src/movie.py`
- The code required to solve questions in Q3 should take a short time to run
  - If with your implementation takes a long time to run, then it should mean that your implementation is not efficient enough, or have some bugs in your code
